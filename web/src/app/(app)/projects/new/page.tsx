import { PhasePlaceholder } from "@/components/shared/PhasePlaceholder";

export default function NewProjectPage() {
  return (
    <PhasePlaceholder
      title="New project"
      phase="F"
      description="Railway-style fast path: pick a repo, kuso detects services + suggests addons, you hit Deploy. Lands in Phase F."
    />
  );
}
