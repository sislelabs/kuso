apiVersion: v1
kind: Service
metadata:
  name: {{ include "kusoapp.fullname" . }}
  labels:
    {{- include "kusoapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kusoapp.selectorLabels" . | nindent 4 }}-web
