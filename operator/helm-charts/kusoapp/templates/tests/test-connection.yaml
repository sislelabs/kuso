apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kusoapp.fullname" . }}-test-connection"
  labels:
    {{- include "kusoapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kusoapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
