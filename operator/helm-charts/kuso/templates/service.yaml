apiVersion: v1
kind: Service
metadata:
  name: {{ include "kuso.fullname" . }}
  labels:
    {{- include "kuso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kuso.selectorLabels" . | nindent 4 }}
