apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kuso.fullname" . }}-test-connection"
  labels:
    {{- include "kuso.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kuso.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
