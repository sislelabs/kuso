# This rule is not used by the project kuso-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to application.kuso.sislelabs.com resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: kuso-operator
    app.kubernetes.io/managed-by: kustomize
  name: kusoaddonmysql-viewer-role
rules:
- apiGroups:
  - application.kuso.sislelabs.com
  resources:
  - kusoaddonmysqls
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - application.kuso.sislelabs.com
  resources:
  - kusoaddonmysqls/status
  verbs:
  - get
