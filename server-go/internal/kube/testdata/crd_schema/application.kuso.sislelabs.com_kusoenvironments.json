{
  "v1alpha1": {
    "description": "KusoEnvironment is one running instance of a KusoService — production or preview.",
    "properties": {
      "apiVersion": {
        "type": "string"
      },
      "kind": {
        "type": "string"
      },
      "metadata": {
        "type": "object"
      },
      "spec": {
        "properties": {
          "additionalHosts": {
            "description": "Extra hostnames routed to this env's pod. Server populates this from KusoService.spec.domains so custom domains attach without losing the auto-generated host.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "autoscaling": {
            "properties": {
              "enabled": {
                "default": false,
                "type": "boolean"
              },
              "maxReplicas": {
                "default": 5,
                "type": "integer"
              },
              "minReplicas": {
                "default": 1,
                "type": "integer"
              },
              "targetCPUUtilizationPercentage": {
                "default": 70,
                "type": "integer"
              }
            },
            "type": "object"
          },
          "branch": {
            "default": "main",
            "type": "string"
          },
          "clusterIssuer": {
            "default": "letsencrypt-prod",
            "type": "string"
          },
          "command": {
            "description": "argv override; only used when runtime == \"worker\".",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "envFromSecrets": {
            "items": {
              "pattern": "^([a-z0-9][a-z0-9-]*-(conn|shared|secrets)|kuso-instance-shared)$",
              "type": "string",
              "x-kubernetes-validations": [
                {
                  "message": "control-plane secrets (kuso-*) cannot be mounted into workload pods",
                  "rule": "self == 'kuso-instance-shared' || !self.startsWith('kuso-')"
                }
              ]
            },
            "type": "array"
          },
          "envOverrides": {
            "description": "Env-var NAMES the user deliberately pinned to this env via the per-env scoped editor. The kuso server's propagation step preserves these env-local values and re-stamps every other service-owned var from the parent KusoService — the explicit marker that tells a deliberate override from a stale inherited seed. Server-managed; the chart does not render it.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "envVars": {
            "items": {
              "properties": {
                "name": {
                  "type": "string"
                },
                "value": {
                  "type": "string"
                },
                "valueFrom": {
                  "properties": {
                    "secretKeyRef": {
                      "properties": {
                        "key": {
                          "type": "string"
                        },
                        "name": {
                          "pattern": "^([a-z0-9][a-z0-9-]*-conn|[a-z0-9][a-z0-9-]*-shared|kuso-instance-shared)$",
                          "type": "string",
                          "x-kubernetes-validations": [
                            {
                              "message": "control-plane secrets (kuso-*) cannot be referenced from workload CRs",
                              "rule": "self == 'kuso-instance-shared' || !self.startsWith('kuso-')"
                            }
                          ]
                        }
                      },
                      "required": [
                        "name",
                        "key"
                      ],
                      "type": "object"
                    }
                  },
                  "type": "object"
                }
              },
              "required": [
                "name"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "healthcheck": {
            "description": "Resolved HTTP health check (propagated from the service). When `path` is set the chart renders HTTP readiness + liveness probes against it instead of the default TCP readiness/startup probes.",
            "properties": {
              "failureThreshold": {
                "type": "integer"
              },
              "initialDelaySeconds": {
                "type": "integer"
              },
              "path": {
                "type": "string"
              },
              "periodSeconds": {
                "type": "integer"
              },
              "port": {
                "type": "integer"
              },
              "timeoutSeconds": {
                "type": "integer"
              }
            },
            "type": "object"
          },
          "host": {
            "type": "string"
          },
          "image": {
            "properties": {
              "pullPolicy": {
                "default": "IfNotPresent",
                "type": "string"
              },
              "repository": {
                "type": "string"
              },
              "tag": {
                "type": "string"
              }
            },
            "type": "object"
          },
          "ingressClassName": {
            "default": "traefik",
            "type": "string"
          },
          "internal": {
            "default": false,
            "description": "When true, skip the Ingress entirely. Service still exists for in-cluster DNS access; no public domain or TLS. Mirrors KusoService.spec.internal.",
            "type": "boolean"
          },
          "kind": {
            "default": "production",
            "enum": [
              "production",
              "preview",
              "custom"
            ],
            "type": "string"
          },
          "pendingImage": {
            "description": "A runtime=image target withheld until the service's release hook (migration) succeeds. Promoted to image by the imagerelease watcher.",
            "properties": {
              "pullPolicy": {
                "type": "string"
              },
              "repository": {
                "type": "string"
              },
              "tag": {
                "type": "string"
              }
            },
            "type": "object"
          },
          "placement": {
            "description": "Inherits the parent service placement so the env-helm chart can render nodeSelector + topologySpread. Pruning was the v0.7.x silent-data-loss bug — schema now covers what server-go writes.",
            "properties": {
              "labels": {
                "additionalProperties": {
                  "type": "string"
                },
                "type": "object"
              },
              "nodes": {
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            },
            "type": "object"
          },
          "platformApiEgress": {
            "default": false,
            "description": "When true, allow this env's pods to call the kuso API over in-cluster DNS. Mirrors KusoService.spec.platformApiEgress.",
            "type": "boolean"
          },
          "port": {
            "format": "int32",
            "type": "integer"
          },
          "privateEgress": {
            "default": false,
            "description": "When true, deny public internet egress for this env's pods. Mirrors KusoService.spec.privateEgress.",
            "type": "boolean"
          },
          "project": {
            "type": "string"
          },
          "publicEnv": {
            "description": "Env var names baked as __KUSO_RUNTIME_\u003cKEY\u003e__ sentinels at build and substituted with the real per-deploy value at pod start. Mirrored from the parent KusoService.spec.publicEnv. Must exist here because the kuso-server build/deploy path writes env.spec.publicEnv; a missing field makes the apiserver silently prune it and the server boots in degraded mode (readyz=503, writes refused).",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "pullRequest": {
            "properties": {
              "headRef": {
                "type": "string"
              },
              "number": {
                "type": "integer"
              }
            },
            "type": "object"
          },
          "release": {
            "description": "Pre-deploy release hook mirrored from\nKusoService.spec.release by the server. The build\npoller reads this off the env CR (already in its\nhot path) when deciding whether to run a release\nJob before promoting an image.\n",
            "properties": {
              "command": {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "timeoutSeconds": {
                "default": 900,
                "type": "integer"
              }
            },
            "type": "object"
          },
          "replicaCount": {
            "default": 1,
            "type": "integer"
          },
          "resources": {
            "type": "object",
            "x-kubernetes-preserve-unknown-fields": true
          },
          "runtime": {
            "description": "Mirrors KusoService.spec.runtime so the env helm chart can specialise (e.g. workers skip Service/Ingress/probes).",
            "type": "string"
          },
          "secretsRev": {
            "type": "string"
          },
          "securityContext": {
            "description": "Opt-in extra Linux capabilities and privilege escalation for images that self-drop root at runtime. Default drops ALL caps with no escalation.",
            "properties": {
              "allowPrivilegeEscalation": {
                "type": "boolean"
              },
              "capabilities": {
                "properties": {
                  "add": {
                    "items": {
                      "type": "string"
                    },
                    "type": "array"
                  }
                },
                "type": "object"
              }
            },
            "type": "object"
          },
          "service": {
            "type": "string"
          },
          "sharedEnvKeys": {
            "description": "Mirrored from the parent KusoService.spec.sharedEnvKeys by the kuso server's propagation step. nil = legacy (chart falls back to envFromSecrets blanket mount); non-nil = chart renders individual env entries per key (per-service opt-in).",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "sleep": {
            "properties": {
              "enabled": {
                "default": false,
                "type": "boolean"
              }
            },
            "type": "object"
          },
          "snapshotBeforeDeploy": {
            "description": "Mirrored from KusoService.spec.snapshotBeforeDeploy. When true and a release hook exists, the poller snapshots subscribed postgres addons before the hook runs.",
            "type": "boolean"
          },
          "spreadPolicy": {
            "description": "How multi-replica pods spread across nodes. \"hard\" = topologySpreadConstraints DoNotSchedule (replicas forced onto distinct nodes); \"soft\"/\"\" = ScheduleAnyway (single-node safe). Server-managed from the live schedulable-node count.",
            "type": "string"
          },
          "stopped": {
            "default": false,
            "description": "When true, hard-stop this env — pin to 0 replicas and do NOT wake on traffic. Mirrors KusoService.spec.stopped. The activator serves a \"service stopped\" 503 for a stopped env instead of scaling it up.",
            "type": "boolean"
          },
          "subscribedAddons": {
            "description": "Mirrored from the parent KusoService.spec.subscribedAddons. nil = legacy auto-mount-all addon-conn secrets; non-nil = operator only includes the listed addons' conn-secrets in envFromSecrets.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "tlsEnabled": {
            "default": true,
            "type": "boolean"
          },
          "tlsHosts": {
            "description": "Subset of {host, additionalHosts...} eligible for a Let's Encrypt cert. Server populates this using the Mozilla public-suffix list — hosts that aren't public FQDNs (e.g. single-label or reserved suffixes like .local) are filtered out so they don't poison the Ingress's all-or-nothing tls block. Unset on legacy CRs falls back to a coarser chart-level check.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "ttl": {
            "properties": {
              "expiresAt": {
                "format": "date-time",
                "type": "string"
              }
            },
            "type": "object"
          },
          "volumes": {
            "description": "Persistent volumes to mount into the env's pod. Same shape as KusoService.spec.volumes; declared explicitly so the apiserver doesn't prune them.",
            "items": {
              "properties": {
                "accessMode": {
                  "type": "string"
                },
                "mountPath": {
                  "type": "string"
                },
                "name": {
                  "type": "string"
                },
                "sizeGi": {
                  "type": "integer"
                },
                "storageClass": {
                  "type": "string"
                }
              },
              "type": "object"
            },
            "type": "array"
          },
          "wildcardDomains": {
            "description": "Wildcard hosts served with a pre-provisioned TLS secret each. Rendered as a separate Ingress without the cert-manager annotation — no per-host LE issuance for covered subdomains.",
            "items": {
              "properties": {
                "host": {
                  "type": "string"
                },
                "tlsSecret": {
                  "type": "string"
                }
              },
              "type": "object"
            },
            "type": "array"
          }
        },
        "required": [
          "project",
          "service"
        ],
        "type": "object"
      },
      "status": {
        "type": "object",
        "x-kubernetes-preserve-unknown-fields": true
      }
    },
    "type": "object"
  }
}
