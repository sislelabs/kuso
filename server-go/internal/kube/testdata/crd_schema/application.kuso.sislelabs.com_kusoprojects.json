{
  "v1alpha1": {
    "description": "KusoProject is the top-level grouping for one product.",
    "properties": {
      "apiVersion": {
        "type": "string"
      },
      "kind": {
        "type": "string"
      },
      "metadata": {
        "type": "object"
      },
      "spec": {
        "properties": {
          "alwaysOn": {
            "default": false,
            "description": "When true, every service in this project runs with\nsleep.enabled=false regardless of per-service sleep\nconfig. Lets a project opt out of scale-to-zero\nglobally — useful when cold-start latency is\nunacceptable (low-traffic but latency-sensitive\nAPIs, background processors that must always be\nlistening).\n",
            "type": "boolean"
          },
          "baseDomain": {
            "description": "Domain under which auto-generated service hostnames are emitted.",
            "type": "string"
          },
          "configAsCode": {
            "description": "Controls the kuso.yaml-on-push behaviour. When enabled (default), a push to the default branch makes kuso fetch kuso.yaml via the GitHub Contents API and apply it before builds run.",
            "properties": {
              "enabled": {
                "default": true,
                "type": "boolean"
              }
            },
            "type": "object"
          },
          "defaultRepo": {
            "properties": {
              "defaultBranch": {
                "default": "main",
                "type": "string"
              },
              "url": {
                "type": "string"
              }
            },
            "type": "object"
          },
          "description": {
            "type": "string"
          },
          "github": {
            "properties": {
              "installationId": {
                "format": "int64",
                "type": "integer"
              }
            },
            "type": "object"
          },
          "namespace": {
            "description": "Optional execution namespace for this project's child\nresources (services, environments, addons, builds,\nplus their per-service Secrets). The KusoProject CR\nitself always lives in the server's home namespace;\nthis field controls only child routing. Empty = home.\n",
            "type": "string"
          },
          "placement": {
            "description": "Project-default node-pinning policy. Services\nwithout their own placement inherit it; new\nenvironments derived from a service inherit the\nservice's effective placement (with project as\nfallback). Empty = schedule anywhere.\n",
            "properties": {
              "labels": {
                "additionalProperties": {
                  "type": "string"
                },
                "description": "Node-label matchers (AND across keys). The\nserver prefixes them with kuso.sislelabs.com/\nbehind the scenes so user-typed labels are\nkept in a separate namespace from kube's.\n",
                "type": "object"
              },
              "nodes": {
                "description": "Specific hostnames the workload may schedule\nonto. Combines with labels via AND: a node\nmust match all labels AND be in this list\n(when set), or match all labels (when empty).\n",
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            },
            "type": "object"
          },
          "previews": {
            "properties": {
              "defaultReviewerEmail": {
                "description": "Address kuso sends the magic-link reviewer URL to when the PR has no `reviewer:\u003cemail\u003e` label. Empty = no email; operator gets the URL only from the dashboard + GitHub PR comment.",
                "type": "string"
              },
              "enabled": {
                "default": false,
                "type": "boolean"
              },
              "triggers": {
                "description": "Gate preview-env spawn on PR target branch. Empty = pre-v0.17 behavior (spawn on every PR). Non-empty = only PRs targeting one of the listed branches spawn a preview; the matched entry's baseEnv selects which existing env to clone env-vars and addon subscriptions from.",
                "items": {
                  "properties": {
                    "baseEnv": {
                      "type": "string"
                    },
                    "branch": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "branch",
                    "baseEnv"
                  ],
                  "type": "object"
                },
                "type": "array"
              },
              "ttlDays": {
                "default": 7,
                "type": "integer"
              }
            },
            "type": "object"
          }
        },
        "type": "object"
      },
      "status": {
        "type": "object",
        "x-kubernetes-preserve-unknown-fields": true
      }
    },
    "type": "object"
  }
}
